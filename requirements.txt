# Python dependencies for home-automation-scripts
#
# Install into the project virtual environment with:
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Or let install.sh handle it automatically.
#
# Add new packages below as the project grows.  Pin versions where stability
# matters; use a minimum version where flexibility is preferred.

# HTTP requests – used by scripts that call local or cloud APIs
requests>=2.31.0

# YAML parsing – used for config files that are not plain .env
PyYAML>=6.0

# python-dotenv – load .env files into the environment
python-dotenv>=1.0.0
